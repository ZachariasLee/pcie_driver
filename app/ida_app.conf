# /etc/ida_app.conf - IDA application configuration
#
# Lines starting with # are comments.
# Format: KEY = VALUE
#
# Keys:
#   IDA_COUNT       Number of IDA channels to use (1-4)
#   BYTES_PER_LINE  Bytes per radar line (2048 * 1.5 = 3072)

IDA_COUNT       = 1
BYTES_PER_LINE  = 3072
